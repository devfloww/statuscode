// Tags and Misc

import chalk from "chalk"

const success = `[${chalk.bold.green('+')}]`
const failure = `[${chalk.bold.red('-')}]`
const process = `[${chalk.bold.blue('*')}]`
const warning = `[${chalk.bold.yellow('!')}]`


export {
  success,
  failure,
  process,
  warning
}